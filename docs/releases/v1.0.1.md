# Release Notes – v1.0.1

## Highlights
- Adopted the GitHub Project board as the single source of truth for backlog management and updated contributor guidance accordingly.
- Added GitHub CLI tooling to the devcontainer so contributors can manage projects and PRs without leaving the container.
- Clarified change-aware synchronization behaviour and strengthened credential guidance and sample environment files.

## Dependency Updates
- Microsoft.Extensions.Options / Configuration.* to 9.0.10
- Serilog and Serilog.Sinks.Console to 4.2.0 / 5.0.1
- Microsoft.NET.Test.Sdk to 18.0.0
- Coverlet (collector & msbuild) to 6.0.4
- GitHub Actions: checkout@v5, setup-dotnet@v5, upload-artifact@v5, docker/build-push-action@v6

## Testing
- `dotnet build`
- `dotnet test --configuration Release --no-build /p:CollectCoverage=true /p:CoverletOutput=TestResults/coverage/ /p:CoverletOutputFormat=cobertura%2copencover /p:Threshold=80 /p:ThresholdType=line /p:ThresholdStat=total`

## Next Steps
- Merge the release PR into `main` to trigger the automated `Release` workflow (GHCR publish + GitHub Release draft).
- After publication, verify GHCR digests and copy these highlights into the GitHub Release body.
